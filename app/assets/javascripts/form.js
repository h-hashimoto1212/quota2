$(function(){
  let aVal;
  let sVal;
  $('input.submit').on('click', function(e){
    // e.preventDefault();

    // aVal = $('input#author').val();
    // sVal = $('input#source').val();
    // console.log(aVal);
    // console.log(sVal);

    // var formData = new FormData(this);
    // var url = $(this).attr('action')

    // $.ajax({
    //   url: url,
    //   type: "POST",
    //   data: {formData, aVal, sVal},
    //   dataType: 'json',
    //   processData: false,
    //   contentType: false
    // })
  })
});